name: nuvigator_flutter_app
description: A new Flutter application.

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2
  nuvigator: ^0.3.0
  build_runner: ^1.7.4

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
